"""Welcome scheduling for pond arrivals."""

def build_welcome_schedule(frogs):
    """Return the complete ordered welcome schedule for every frog."""
    return []


def greet_next(frogs):
    schedule = build_welcome_schedule(frogs)
    return schedule[0] if schedule else None
