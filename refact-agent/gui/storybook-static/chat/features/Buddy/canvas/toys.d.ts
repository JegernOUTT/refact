import type { BuddyAnimState, ColorMap } from "../types";
export declare function renderWalkingFeet(ctx: CanvasRenderingContext2D, ox: number, oy: number, spriteWidth: number, spriteHeight: number, m: ColorMap, anim: BuddyAnimState): void;
export declare function renderToy(ctx: CanvasRenderingContext2D, ox: number, oy: number, spriteWidth: number, spriteHeight: number, m: ColorMap, anim: BuddyAnimState): void;
