# v8.2.8

- Fixed a test timeout flake in the JetBrains plugin CI (LSPProcessHolder lifecycle
  tests need more than 3s on loaded runners; raised to 10s).
- Websocket transport disabled for OpenAI Codex (provider-side connectivity change).
- Goal-validator messages no longer block the generation stream after validation.
- Release process: CI now assembles engine release assets from the same artifact
  pipeline that plugin releases always used, eliminating build-matrix divergence.
